name: tests

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: neuroinformatics-unit/actions/lint@v2

  test:
    needs: [linting]
    name: ${{ matrix.os }} ${{ matrix.qt-backend }} py${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}

    defaults:
      run:
        shell: bash -l {0}

    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]
        qt-backend: [pyqt5, pyqt6, pyside6]

    steps:
      - uses: actions/checkout@v4

      - name: Install Ubuntu Qt runtime libraries
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libxkbcommon-x11-0

      - name: Install viewephys from repo
        run: |
          python -m pip install --upgrade pip
          pip install .[dev,${{ matrix.qt-backend }}]

      - name: Run tests
        env:
          QT_QPA_PLATFORM: offscreen
          MPLCONFIGDIR: /tmp/matplotlib
        run: |
          pytest

#  build_sdist_wheels:
#    name: Build source distribution
#    needs: [test]
#    if: github.event_name == 'push' && github.ref_type == 'tag'
#    runs-on: ubuntu-latest
#    steps:
#    - uses: neuroinformatics-unit/actions/build_sdist_wheels@v2


#  upload_all:
#    name: Publish build distributions
#    needs: [build_sdist_wheels]
#    if: github.event_name == 'push' && github.ref_type == 'tag'
#    runs-on: ubuntu-latest
#    steps:
#    - uses: actions/download-artifact@v4
#      with:
#        name: artifact
#        path: dist
#    - uses: pypa/gh-action-pypi-publish@release/v1
#      with:
#        user: __token__
#        password: ${{ secrets.TWINE_API_KEY }}
